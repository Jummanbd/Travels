// import { TravelPackage } from "@/types/data_type";
// import fs from "fs";
// import path from "path";

// export function api_set(): TravelPackage[] {
//   const filePath = path.join(process.cwd(), "data", "data.json");
//   const jsonData = fs.readFileSync(filePath, "utf-8");
//   return JSON.parse(jsonData) as TravelPackage[];
// }
